import {searchProjects} from './matcher.js';
export function fallbackConcept(type,idea,projects){const systems={Website:['Content structure','Responsive pages','Enquiry flow','Content management'],'Mobile App':['Mobile navigation','Core user workflow','Local preferences','Release preparation'],AI:['Input workflow','AI service integration','Output review','Usage controls'],Marketplace:['Customer accounts','Supplier listings','Search and filters','Admin dashboard'],'Booking Platform':['Availability calendar','Booking requests','Customer notifications','Booking administration'],'E-commerce':['Product catalogue','Shopping cart','Checkout integration','Order management'],'Something Crazy':['Concept validation','Interactive prototype','Core experience','Feedback collection']};const core=systems[type]||systems.Website;const matches=searchProjects(type+' '+idea,projects).projects;return{projectName:`Your ${type.toLowerCase()} concept`,summary:idea,productType:type,coreSystems:core,suggestedPhases:[{name:'MVP',items:core.slice(0,3)},{name:'Phase 2',items:[core[3],'Refine based on user feedback']}],complexity:null,matchingProjectIds:matches.map(p=>p.id),nextStep:'Discuss the scope, users and requirements with DevMania.'}}
export function validConcept(x,projects){return x&&typeof x.projectName==='string'&&x.projectName.length<=160&&typeof x.summary==='string'&&x.summary.length<=4000&&typeof x.productType==='string'&&Array.isArray(x.coreSystems)&&x.coreSystems.length<=12&&x.coreSystems.every(s=>typeof s==='string'&&s.length<=250)&&Array.isArray(x.suggestedPhases)&&x.suggestedPhases.length<=4&&x.suggestedPhases.every(p=>typeof p.name==='string'&&Array.isArray(p.items)&&p.items.every(s=>typeof s==='string'&&s.length<=250))&&Array.isArray(x.matchingProjectIds)&&x.matchingProjectIds.every(id=>projects.some(p=>p.id===id))&&typeof x.nextStep==='string'&&(!x.complexity||(Number.isInteger(x.complexity.score)&&x.complexity.score>=1&&x.complexity.score<=10&&['low','medium','high'].includes(x.complexity.level)&&typeof x.complexity.explanation==='string'))}
export function mountBuilder({dialog,content},{projects,initial,contact,focusProject,track,el}){let step=1,type='Website',idea=initial;let cancelled=false;dialog.addEventListener('close',()=>cancelled=true);const controller=new AbortController();dialog.addEventListener('close',()=>controller.abort());
 function typeStep(){content.replaceChildren(el('p','01 / What are we building?','os-step'));const options=el('div',undefined,'os-type-options');for(const t of ['Website','Mobile App','AI','Marketplace','Booking Platform','E-commerce','Something Crazy']){const b=el('button',t);b.type='button';b.setAttribute('aria-pressed',String(t===type));b.onclick=()=>{type=t;ideaStep()};options.append(b)}content.append(options)}
 function ideaStep(){step=2;content.replaceChildren(el('p',`02 / Your ${type.toLowerCase()} idea`,'os-step'));const form=el('form');const label=el('label','Tell me your idea in one or two sentences');const field=el('textarea');field.rows=5;field.maxLength=2000;field.minLength=10;field.required=true;field.value=idea;label.append(field);const users=el('label','Who is it for? (optional)');const audience=el('input');audience.maxLength=200;users.append(audience);const back=el('button','Back','os-close');back.type='button';back.onclick=()=>{idea=field.value;typeStep()};const go=el('button','Create project outline','button');form.append(label,users,back,go);content.append(form);field.focus();form.onsubmit=async e=>{e.preventDefault();idea=field.value;go.disabled=true;go.textContent='Preparing your outline…';const brief=idea+(audience.value?'\nTarget users: '+audience.value:'');let concept,ai=false;const timeout=setTimeout(()=>controller.abort(),12000);try{const r=await fetch('/api/idea',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({productType:type,idea:brief}),signal:controller.signal});if(!r.ok)throw Error();const x=await r.json();if(!validConcept(x,projects))throw Error();concept=x;ai=true}catch{concept=fallbackConcept(type,brief,projects);track('idea_builder_failed',{productType:type})}finally{clearTimeout(timeout)}if(!cancelled)result(concept,ai)}}
 function result(c,ai){track('idea_builder_completed',{productType:type,resultCount:c.matchingProjectIds.length});content.replaceChildren(el('p',ai?'PROJECT CONCEPT':'PROJECT STARTER','os-step'),el('h3',c.projectName),el('p',c.summary));content.append(el('p',ai?'A starting point for discussion, not a quotation or delivery commitment.':'AI generation is currently unavailable. Your idea is preserved below with a suggested starting checklist. You can edit it and continue to an enquiry.','os-notice'));content.append(el('h3','Suggested core systems'));const systems=el('ul');c.coreSystems.forEach(s=>systems.append(el('li',s)));content.append(systems);for(const phase of c.suggestedPhases){content.append(el('h3',phase.name));const ul=el('ul');phase.items.forEach(s=>ul.append(el('li',s)));content.append(ul)}content.append(el('h3','Related DevMania experience'));if(!c.matchingProjectIds.length)content.append(el('p','No close portfolio match was found. We can still discuss your idea.'));for(const id of c.matchingProjectIds){const p=projects.find(p=>p.id===id);const b=el('a',p.name,'os-related');b.href='#project-'+p.id;b.onclick=e=>{e.preventDefault();track('idea_project_match_clicked');focusProject(p)};content.append(b)}if(c.complexity){content.append(el('h3',`Build complexity: ${c.complexity.score}/10`),el('p',c.complexity.explanation));const meter=el('meter');meter.min=1;meter.max=10;meter.value=c.complexity.score;meter.setAttribute('aria-label','Build complexity');content.append(meter)}else content.append(el('p','Complexity will be assessed once the scope is reviewed.'));
 const label=el('label','Your editable project brief');const brief=el('textarea');brief.rows=8;brief.maxLength=5000;brief.value=`${c.projectName}\n\n${c.summary}\n\nSuggested systems:\n${c.coreSystems.map(x=>'- '+x).join('\n')}\n\nSuggested phases:\n${c.suggestedPhases.map(p=>p.name+': '+p.items.join(', ')).join('\n')}\n\nNext step: ${c.nextStep}`;label.append(brief);content.append(label);const go=el('button','Start this project','button');go.onclick=()=>{track('start_project_clicked',{productType:type});contact(brief.value,type)};const edit=el('button','Edit my idea','os-close');edit.onclick=ideaStep;content.append(go,edit)}
 typeStep();
}
